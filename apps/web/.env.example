# Session 18: no browser API origin is needed.
# fetch('/api/...') uses the same origin (Next :3000).
# next.config.ts forwards /api/* to Nest http://localhost:4000/api/*.
# Optional server-only override for an isolated worktree (set before dev/build).
# API_ORIGIN=http://localhost:4022
