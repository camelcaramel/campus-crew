# Copy to apps/web/.env.local. NEXT_PUBLIC_ values are visible in the browser.
# Reserved for a future API client; the current page does not call the API.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
