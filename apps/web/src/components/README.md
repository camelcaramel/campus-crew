# components — 여러 기능에서 사용하는 UI

버튼, 입력창 같은 공통 UI와 공통 헤더를 둡니다. 예: `ui/button.tsx`, `layout/header.tsx`.

특정 기능 전용인 모집글 카드는 `features/recruitments/`에 둡니다. URL에 해당하는 페이지는 기존 `app/`에 유지합니다.

5차시에는 역할만 정합니다. 실제 컴포넌트와 하위 폴더는 사용하는 차시에 추가합니다.
