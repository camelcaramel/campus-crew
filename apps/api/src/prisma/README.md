# prisma — NestJS와 데이터베이스 연결

데이터베이스 차시에 NestJS용 `prisma.module.ts`, `prisma.service.ts`를 둡니다.

스키마와 마이그레이션은 이 폴더와 구분하여 API 앱 루트의 `apps/api/prisma/`에 둡니다.
예: `apps/api/prisma/schema.prisma`.

5차시에는 Prisma 의존성, DB 연결, 스키마를 만들지 않습니다. 실제 연결을 배우는 차시에 추가합니다.
